spring:
  application:
    name: organizationservice
  profiles:
    active:
      default
  cloud:
    config:
      uri: http://localhost:9999
      server:
        encrypt:
          enabled: false
encrypt:
  key: GUCHEN7364
